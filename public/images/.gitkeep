# TODO: Add the following images to this directory:
# - hero.jpg          (hero background — dog walking in Tel Aviv street)
# - dogwalking.jpg    (dogwalking service section image)
# - dogsitting-home.jpg   (dogsitting at client's home section image)
# - dogsitting-my-place.jpg  (dogsitting at owner's home section image)
#
# Recommended size: at least 1200×900px, JPG or WebP.
# The site will display correctly even without these images (teal/gray background fallback).
